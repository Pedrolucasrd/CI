name: maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
  
      runs-on: ubuntu-latest
  
      steps:
      
      - name: Configure SSH
        uses: appleboy/ssh-action@master
        with:
          host: 187.60.56.72
          username: Administrator
          password: UCTech2023*
          script: |
            cd teste123
            mkdir entrei
